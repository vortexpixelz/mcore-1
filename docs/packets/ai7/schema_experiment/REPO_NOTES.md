# Repo Notes

This experiment currently lives under `docs/packets/ai7/schema_experiment/`.

If it becomes reusable beyond AI7, promote the winning template to a broader `docs/schemas/` or `src/` location later.
