# Promote After

After the result memo is complete, promote the winner to a reusable template.

Suggested future path:

```text
docs/packets/ai7/AI7_PACKET_TEMPLATE.md
```

Only promote after the experiment is scored.
