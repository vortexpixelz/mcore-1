# Artifact Target

The final artifact is not merely a summary.

The final artifact is a reusable AI7 packet template that can be applied to future dated bundles.

Minimum viable winner:

```yaml
schema_id:
schema_version:
required_fields:
claim_tier_policy:
scorecard_result:
next_action_rule:
```
